#include<bits/stdc++.h>
using namespace std;

class Solution {
public:
    string addBinary(string a, string b) {
        int sizeOfa=a.size();
        int sizeOfb=b.size();
        int carry='0';
        string ans="";
        while(sizeOfa==0 || sizeOfb==0){
            if(a[sizeOfa-1]=='1')
        }
    }
};